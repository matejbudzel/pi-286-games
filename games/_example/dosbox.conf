[sdl]
fullscreen=true

[render]
aspect=true

[joystick]
joysticktype=auto

[autoexec]
# The launcher is expected to supply the mount/start commands or generate
# an equivalent per-game invocation. Keep game binaries outside this repo.
